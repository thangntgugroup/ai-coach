import type { MediaUpload } from '@/types/diagnostic.type';

/** Stand-in for a recording the user submitted, until capture + upload are wired up. */
export const MEDIA_UPLOAD: MediaUpload = {
  fileName: '1on1_Trang_0902.mp4',
  duration: '7:48',
  note: 'Nhận diện 2 người nói. Tiếng rõ.',
};
